package com.algaworks.algalog.domain;

// Interface para agrupar grupos de validação
public interface ValidationGroups {
    public interface ClientId { } // Não tem métodos por ser uma interface de marcação
}
